SUM(REWARD(event.data.reward_id).faceValue,step_name:\"reward_earned\")
sum(manual_counter)
sum(regular_counter)
sum(collection(person(event.personId).rewards, filter:faceValueType == Reward Face Value, extracting:faceValue))
SUM(event.data.amount, step_name:\"converted\")
SUM(event.data.amount, step_name:\"advocate_reward_redeemed\")
SUM(event.data.amount, step_name:\"converted\", visit_type:\"new_to_client\")
SUM(event.data.amount, step_name:\"converted\", visit_type:\"new_to_program\")
sum(event.faceValue)
sum(boolean_format(event.type == \"reward_earned\" and reward_state == \"canceled\",event.faceValue,\"0\"))
sum(boolean_format(event.type == \"reward_earned\" and reward_state == \"failed\",event.faceValue,\"0\"))
sum(boolean_format(event.type == \"reward_earned\" and reward_state not like failed_or_canceled_regex,event.faceValue,\"0\"))
sum(collection(person_rewards_matching_dimensions, filter: dateEarned like current_year and state not like failed_or_canceled_regex, extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\" and dateEarned >= one_year_ago, extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\", extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\" and dateEarned >= one_year_ago, extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\", extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned like previous_year, extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned >= one_year_ago, extracting: faceValue))
sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex, extracting: faceValue))
sum(date_format(now(), format:\"epoch\"), \"-31536000000\")
sum( boolean_format(is_makegood == \"true\", REWARD(event.rewardId).faceValue, \"null\" ))
sum(REWARD(event.rewardId).faceValue)
sum(date_format(report_parameters().timeRange.endTime, format:\"epoch\"), \"-604800000\")
