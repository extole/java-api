DATE_FORMAT(STEP_RECORD(event.earnedStepEventContext.id, step_name: event.earnedStepEventContext.name, event_time_name: event.earnedStepEventContext.eventTime).eventTime, format:yyyy/MM/dd)
DATE_FORMAT(event.eventTime, format:\"epoch\")
date_format(END_DATE(event.eventTime, period:report_parameters().period), format:\"yyyy-MM-dd\")
date_format(first(collection(person(event.personId).steps, filter: stepName==\"signed_up\" and container == event.container)).createdDate, format:MM/dd/yyyy)
date_format(person(event.data.related_person_id).data.pm_approval_date, format:MM/dd/yyyy)
date_format(person(event.data.related_person_id).data.terms_accepted, format:MM/dd/yyyy)
date_format(person(event.personId).data.terms_accepted, format:MM/dd/yyyy)
date_format(period_start_time, format: \"epoch\")
date_format(person_collection(event.personId, collection:\"steps\", filter: campaignId == \"null\" and stepName == \"converted\" and eventDate <= period_start_time, extracting: eventDate, reduce:\"max\"), format: \"epoch\")
date_format(event.rewardRedeemedDate)
date_format(event.rewardEarnedDate)
date_format(event.rewardFulfilledDate)
DATE_FORMAT(event.eventTime)
DATE_FORMAT(event.rewardEarnedDate)
date_format(now(), format:\"epoch\")
date_format(now(), format:\"yyyy\", timezone:UTC)
date_format(one_year_ago, format:\"yyyy\", timezone:UTC)
date_format(replace(concatenate(\"\",number_format(sum(date_format(now(), format:\"epoch\"), \"-31536000000\"))),search:\".00\",replacement:\"\"),format:\"yyyy-MM-dd HH:mm:ss\",timezone:UTC)
date_format(END_DATE(event.eventTime,period:report_parameters().period),format:\"yyyy-MM-dd\")
date_format(END_DATE(event.eventTime, period:\"TRAILING_WEEK\"), format:\"yyyy-MM-dd\")
date_format(replace(concatenate(\"\", number_format(sum(date_format(report_parameters().timeRange.endTime, format:\"epoch\"), \"-604800000\"))), search:\".00\", replacement:\"\"), format:\"yyyy-MM-dd HH:mm:ss\", timezone:UTC)
date_format(report_parameters().timeRange.endTime, format:\"epoch\")
date_format(event.eventTime)
