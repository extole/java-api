last(\"300\")
last(count(collection(split(concatenate_collection(PERSON(event.personId).recentRequestContexts,extracting: geoIp.country.name,joinBy:\",\",unique:\"true\"),separator:\",\"),filter: value !=\"\")))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: dateEarned like current_year and state not like failed_or_canceled_regex, extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\" and dateEarned >= one_year_ago, extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\", extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\" and dateEarned >= one_year_ago, extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\", extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned like previous_year, extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned >= one_year_ago, extracting:id)))
last(count_distinct(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex, extracting:id)))
last(\".*Eligible for High Rewards.*\")
last(sum(collection(person_rewards_matching_dimensions, filter: dateEarned like current_year and state not like failed_or_canceled_regex, extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\" and dateEarned >= one_year_ago, extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state==\"CANCELED\", extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\" and dateEarned >= one_year_ago, extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state==\"FAILED\", extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned like previous_year, extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex and dateEarned >= one_year_ago, extracting: faceValue)))
last(sum(collection(person_rewards_matching_dimensions, filter: state not like failed_or_canceled_regex, extracting: faceValue)))
LAST(PERSON(event.personId).identityKey, step_name:\"application_approved\")
LAST(PERSON(event.personId).identityKeyValue, step_name:\"application_approved\")
last(date_format(replace(concatenate(\"\", number_format(sum(date_format(report_parameters().timeRange.endTime, format:\"epoch\"), \"-604800000\"))), search:\".00\", replacement:\"\"), format:\"yyyy-MM-dd HH:mm:ss\", timezone:UTC))
LAST(PERSON(event.personId).email, step_name:\"application_approved\")
LAST(PERSON(event.personId).firstName, step_name:\"application_approved\")
LAST(PERSON(event.personId).id, step_name:\"application_approved\")
LAST(PERSON(event.personId).lastName, step_name:\"application_approved\")
LAST(PERSON(event.personId).partnerUserId, step_name:\"application_approved\")
LAST(PERSON(event.personId).profilePictureUrl, step_name:\"application_approved\")
last(\"false\")
last(\"true\")
